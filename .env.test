# This file should only be modified to add new env variables.
# If you need to use different values than the ones here, create a .env.test.local file and overwrite the values over there.
# You only need to overwrite values that are different than the ones here.
OAUTH_CLIENT_ID=TOKEN
OAUTH_CLIENT_SECRET=SECRET
OAUTH_REGISTRATION_ACCESS_TOKEN=REGISTRATION_TOKEN
